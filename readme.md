Start the sample application via
`npm start`

Run test via
`npm test`

Do not use
`node app.js`
to start sample application as babel won't be used to compile ES6 syntax

Go to localhost:3000/news
and see the API results in console
